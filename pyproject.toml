[project]
name = "codex-agent-loop"
version = "0.1.0"
description = "Local Git-backed Codex task loop MVP"
requires-python = ">=3.11"

[project.scripts]
agent-loop = "agent_loop.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
